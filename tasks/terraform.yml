version: '3'

silent: true

env:
  TFLINT_CONFIG:
    sh: |
      if [[ -f $GITHUB_WORKSPACE/.tflint.hcl ]]; then
        echo $GITHUB_WORKSPACE/.tflint.hcl
      else
        echo $GITHUB_ACTION_PATH/configs/.tflint.hcl
      fi
  TRIVY_CONFIG:
    sh: |
      if [[ -f $GITHUB_WORKSPACE/.trivy.yml ]]; then
        echo $GITHUB_WORKSPACE/.trivy.yml
      else
        echo $GITHUB_ACTION_PATH/configs/.trivy.yml
      fi

output:
  group:
    begin: '::group::{{.TASK}}'
    end: '::endgroup::'

tasks:
  # Installations
  install_terraform:
    name: Install Terraform
    ignore_error: true
    run: once
    internal: true
    cmds:
      - bash $GITHUB_ACTION_PATH/scripts/install_terraform.sh
    status:
      - bash $GITHUB_ACTION_PATH/scripts/evaluate.sh $UCI_TERRAFORM_FMT_CHECKS
  install_tflint:
    name: Install TFLint
    ignore_error: true
    run: once
    internal: true
    cmds:
      - |
        curl -s https://raw.githubusercontent.com/terraform-linters/tflint/master/install_linux.sh | bash
        tflint --config {{.TFLINT_CONFIG}} --init
    status:
      - bash $GITHUB_ACTION_PATH/scripts/evaluate.sh $UCI_TERRAFORM_TFLINT_CHECKS
  install_trivy:
    name: Install trivy
    ignore_error: true
    run: once
    internal: true
    cmds:
      - curl -sfL https://raw.githubusercontent.com/aquasecurity/trivy/main/contrib/install.sh | sudo sh -s -- -b /usr/local/bin $UCI_TERRAFORM_TRIVY_VERSION
    status:
      - bash $GITHUB_ACTION_PATH/scripts/evaluate.sh $UCI_TERRAFORM_TRIVY_CHECKS

  # Checks
  run_terraform_fmt:
    name: Run terraform fmt
    ignore_error: true
    run: once
    deps: [install_terraform]
    cmds:
      - terraform fmt -recursive $GITHUB_WORKSPACE
    status:
      - bash $GITHUB_ACTION_PATH/scripts/evaluate.sh $UCI_TERRAFORM_FMT_CHECKS
  run_tflint:
    name: Run TFLint
    ignore_error: true
    run: once
    deps: [install_tflint]
    cmds:
      - tflint --config {{.TFLINT_CONFIG}} --recursive
    status:
      - bash $GITHUB_ACTION_PATH/scripts/evaluate.sh $UCI_TERRAFORM_TFLINT_CHECKS
  run_trivy:
    name: Run Trivy
    ignore_error: true
    run: once
    deps: [install_trivy]
    cmds:
      - trivy config -c {{.TRIVY_CONFIG}} $GITHUB_WORKSPACE/
    status:
      - bash $GITHUB_ACTION_PATH/scripts/evaluate.sh $UCI_TERRAFORM_TRIVY_CHECKS

  default:
    ignore_error: true
    deps:
      - run_terraform_fmt
      - run_tflint
      - run_trivy
